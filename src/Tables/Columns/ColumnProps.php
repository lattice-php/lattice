<?php

declare(strict_types=1);

namespace Lattice\Lattice\Tables\Columns;

/**
 * Marker for a column's type-specific props value object. Each implementation is
 * generated into the `ColumnProps` TypeScript map keyed by its column's wire type.
 */
interface ColumnProps {}
