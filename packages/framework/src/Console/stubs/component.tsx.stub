import type { RendererComponent } from "@lattice-php/lattice";

export const {{ class }}Component: RendererComponent<"{{ type }}"> = ({ node, children }) => {
  // Render the {{ type }} component. Child nodes (if any) are in `children`.
  return <div data-test={node.id}>{children}</div>;
};
