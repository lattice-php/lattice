import { BlockStyle, BlockSupports } from "../../types";
export declare function AdvancedPanel({
  id,
  style,
  supports,
}: {
  id: string;
  style: BlockStyle;
  supports: BlockSupports;
}): import("react").JSX.Element;
