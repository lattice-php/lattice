import { BlockTarget } from "../../types";
export declare function InsertMenu({
  target,
  label,
  compact,
}: {
  target: BlockTarget;
  label: string;
  compact?: boolean;
}): import("react").JSX.Element | null;
