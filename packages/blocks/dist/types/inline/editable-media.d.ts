import { Node } from "@lattice-php/core";
import { BlockBinding } from "./use-block-binding";
/** An image bound to a media field: the rendered picture, with the picker one click away. */
export declare function EditableMedia({
  node,
  binding,
}: {
  node: Node;
  binding: BlockBinding;
}): import("react").JSX.Element;
